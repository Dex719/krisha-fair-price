# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
build/
dist/

# Данные и артефакты (не коммитим — большие)
data/*.db
data/*.db-shm
data/*.db-wal
# файловая блокировка старта воркеров (см. _startup_lock в api/app.py)
data/.startup.lock
data/*.csv
# ревизия сборки: пишется воркфлоу деплоя прямо перед пушем снапшота в Space
data/build_revision.txt
data/rescrape_history_*.json
# модель и метаданные коммитим — нужны деплою на Railway

# reports/*.png — SHAP-график коммитим, он нужен README

# но папки сохраняем
!data/.gitkeep
!models/.gitkeep
!reports/.gitkeep

# Окружение
.env
.DS_Store
.idea/
.vscode/

# catboost
catboost_info/

# Локальный конфиг превью для Claude Code (пути к venv у каждого свои)
.claude/
