# Changelog

Формат: [semver](https://semver.org) `0.MINOR.PATCH` — MINOR за фичи, PATCH за фиксы.

Чек-лист релиза: поднять версию в `pyproject.toml` и `src/krisha/__init__.py`,
обновить подвалы `static/index.html` и `static/stats.html`, добавить запись сюда,
после мержа повесить git-тег `vX.Y.Z`.

## Unreleased

- Производительность HF Spaces (#58): `shap`/`matplotlib` вынесены из
  runtime-зависимостей в optional extras `train`/`dev`; в Dockerfile ограничены
  native thread pools под 2 vCPU (OMP/OpenBLAS/MKL/NumExpr); на startup — fail-soft
  прогрев runtime-кэшей (CatBoost-модель, interval-модели, spatial reference,
  OSM POI index), чтобы первый `/api/predict` не платил за cold start
- `.dockerignore`: из Docker build context исключены доки, ноутбуки, тесты,
  кэши и локальные данные — быстрее билд, меньше риск утащить лишнее в образ

Фиксы по итогам аудита (см. `docs/AUDIT-2026-07-03.md`):

- **Приватность**: `subscriptions.json` и `tracked.json` теперь шифруются целиком
  (Fernet; ключ — `STATE_ENCRYPTION_KEY` или производный от `TELEGRAM_BOT_TOKEN`),
  chat_id больше не попадает ни в файлы публичного репозитория, ни в commit messages
- **Надёжность**: короткие бюджеты ретраев для пользовательского пути —
  `/api/predict` и бот больше не ждут бэкоффы краулера (до минут) в потоке запроса;
  `/api/flags` делает ровно один живой запрос к Gemini без sleep-ретраев
- **Целостность данных**: `db_release` проверяет sha256-контрольную сумму базы,
  если она опубликована рядом с ассетом релиза; `source` объявления больше не
  перезаписывается пользовательским предиктом (происхождение первой записи сохраняется)
- **Корректность**: текущий год вычисляется, а не захардкожен (`building_age`
  больше не «поедет» 1 января); лимит тела запроса 64 КБ и `max_length` на URL
- **Производительность**: кэш LLM-флагов при обучении читается одним соединением
  пачкой вместо 7000+ отдельных connect'ов к SQLite
- **Код**: deprecated `@app.on_event` заменён на lifespan (с сохранением warmup из #58);
  токен бота вырезается из логов ошибок Telegram; ruff-правила `B` (bugbear)
- **Проект**: LICENSE (MIT), `.env.example` со всеми переменными окружения,
  Dependabot (pip + actions), тесты на шифрование состояния

## 0.2.0 — 2026-07-03

Переезд и большой апдейт.

- Ребрендинг: продукт теперь называется просто **FairPrice**
- Переезд с Railway на Hugging Face Spaces (бесплатно), автодеплой при пуше в main
- Починен Telegram Mini App (CSP `frame-ancestors` не пускал Telegram) и бот
  (webhook не регистрировался — теперь ретраи + самолечение через health-пинг)
- Срок продажи v1: «похожие снимают с продажи за ~N дней» в API, на сайте и в боте —
  медианы по сегменту район+комнаты и по полосам отклонения цены от справедливой
- Сбор данных по аренде: `rescrape.py --deal arenda` → отдельная база `krisha_rent.db`
- Честная валидация при обучении (early stopping без подглядывания в тест) и гейт
  сравнения со старой моделью на общем свежем тесте
- Надёжность: WAL-режим SQLite, индекс по fingerprint, webhook-дедупликация,
  фильтры активных объявлений в статистике, полный UPSERT при рескрейпе
- UX: форма оценки в одну линию с иконкой-кнопкой, актуальные метрики в подвале,
  версия на сайте и в `/api/health`
- Ежедневный рескрейп продажи (06:00 Алматы) и аренды (20:00), еженедельное
  переобучение, keepalive-пинг Space

## 0.1.0 — 2026-06

Первый релиз (Railway).

- CatBoost-модель справедливой цены: 7 000+ объявлений, честный сплит по зданиям,
  MAPE 9.5%, SHAP-факторы, конформный интервал цены
- Сайт с вердиктом и дашбордом рынка Алматы, светлая/тёмная тема
- Telegram-бот с вердиктом по ссылке и слежкой за ценой (/track)
- Вежливый resumable-краулер krisha.kz → SQLite
